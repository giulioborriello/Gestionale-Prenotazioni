spring.application.name=formerjob

spring.datasource.url=jdbc:postgresql://localhost:5432/formerjob
spring.datasource.username=postgres
spring.datasource.password=root

# PRIMA VOLTA!
spring.jpa.hibernate.ddl-auto=create
# SUCCESSIVE VOLTE!
#spring.jpa.hibernate.ddl-auto=update

# PRIMA VOLTA!
spring.sql.init.mode=always
# SUCCESSIVE VOLTE!
#spring.sql.init.mode=never

# PRIMA VOLTA!
spring.jpa.defer-datasource-initialization=true
# SUCCESSIVE VOLTE!
#spring.jpa.defer-datasource-initialization=false

spring.jpa.show-sql=false

spring.sql.init.encoding=UTF-8
spring.sql.init.platform=postgresql
spring.sql.init.data-locations=classpath:data.sql
spring.sql.init.continue-on-error=false

spring.jpa.properties.hibernate.hbm2ddl.create_namespaces=true

# ============================
#  Mailtrap SMTP (test only)
# ============================
spring.mail.host=sandbox.smtp.mailtrap.io
spring.mail.port=2525
spring.mail.username=1ddebea66136f7
spring.mail.password=a235daae18a536
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000
spring.mail.properties.mail.smtp.debug=true
spring.mail.default-encoding=UTF-8

# Mittente logico
app.mail.from=prova@gestionale.local

# Profilo attivo - cambia in "prod" per produzione
spring.profiles.active=dev